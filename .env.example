# The real Aura backend. This is the default already baked into
# src/lib/api.ts, so .env is only needed to point at a different
# environment (local dev backend, staging, etc).
VITE_API_BASE_URL=https://auro-backend-api.onrender.com/api
